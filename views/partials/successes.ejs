<% if (locals.successes) { %>
  <ul class="flex flex-col gap-3">
    <% successes.map(success => { %>
      <li class="bg-green-300 px-3 py-1 rounded-lg"><%= success.msg %></li>
    <% }) %>
  </ul>
<% } %>